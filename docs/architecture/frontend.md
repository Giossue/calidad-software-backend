# Frontend

La interfaz ya no forma parte de este repositorio. Vive en
`Giossue/calidad-software-frontend` y consume la API `/api/v1`.

Los contratos JSON se mantienen mediante Resources y pruebas de feature. Los
cambios incompatibles requieren una nueva versión de la API o una migración
coordinada que permita desplegar backend y frontend de forma independiente.
